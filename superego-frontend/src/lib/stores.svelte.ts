import { persistedLocalState } from './utils/persistedLocalState.svelte';
// AppStateStore moved to src/lib/state/app.svelte.ts

// Legacy persisted stores (persistedKnownThreadIds, persistedUiSessions, persistedActiveSessionId)
// have been removed and replaced by SessionStateStore in src/lib/state/session.svelte.ts
